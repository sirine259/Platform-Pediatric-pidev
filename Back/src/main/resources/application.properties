spring.application.name=kidneytransplant-forum-service

# Database Configuration - H2 file-based persistence
spring.datasource.url=jdbc:h2:file:./data/pediatricdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Server Configuration
server.port=8091

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN

# JWT Configuration
jwt.secret=pediatricPlatformSecretKey2024
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Mail Configuration - Mailtrap
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=b8e4801a0b293e
spring.mail.password=e58b7b5834ff08
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.from.email=sirine.hamzaoui@esprit.tn
spring.mail.from.name=Plateforme P̩diatrique

# Email Templates
spring.mail.templates.forgot-password.subject=R̩initialisation de votre mot de passe - Plateforme P̩diatrique
spring.mail.templates.forgot-password.template=forgot-password
spring.mail.templates.password-reset.subject=Confirmation de r̩initialisation - Plateforme P̩diatrique
spring.mail.templates.password-reset.template=password-reset

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:4200
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.esprit.platformepediatricback=DEBUG
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# Eureka Client Configuration (enabled for microservices)
eureka.client.enabled=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# Application Configuration
app.name=Pediatric Platform
app.version=1.0.0
app.description=Comprehensive Pediatric Healthcare Platform

# DevTools Configuration
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# Chatbot OpenAI Configuration
openai.api.key=votre-cle-key
openai.api.url=https://api.openai.com/v1/chat/completions

# Google Maps API Configuration
google.maps.api.key=AIzaSyAgl_7xx-xosndpAcEG4v0ZG1r0mVpDZmk
